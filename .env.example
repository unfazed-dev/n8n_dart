# n8n_dart Environment Variables Example
# Copy this file to .env or .env.test and fill in your values
# DO NOT commit .env or .env.test with real credentials!

# ============================================================================
# n8n API Configuration (Required for integration tests)
# ============================================================================
N8N_BASE_URL=https://your-instance.n8n.cloud
N8N_API_KEY=your-n8n-api-key

# ============================================================================
# Workflow Generator Credentials (Optional - for automatic credential injection)
# Uncomment the credentials you want to use in generated workflows
# ============================================================================

# PostgreSQL Database (used by 18/19 example workflows)
# Required for: CRUD API, User Registration, Multi-Step Forms, etc.
# SUPABASE_DB_HOST=db.your-project.supabase.co
# SUPABASE_DB_PASSWORD=your-database-password

# Supabase API (used by 3 example workflows)
# Required for: Supabase Booking System workflows
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_KEY=your-supabase-anon-key

# Email/SMTP (used by 12 example workflows)
# Required for: User Registration, Order Processing, Notifications, etc.
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=noreply@yourdomain.com

# Slack (used by 10 example workflows)
# Required for: File Upload, Alert Systems, Notifications, etc.
# SLACK_TOKEN=xoxb-your-slack-bot-token
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL
# SLACK_DEFAULT_CHANNEL=#general

# Stripe Payment Gateway (used by 3 example workflows)
# Required for: Order Processing, Booking with Payment workflows
# STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
# STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# AWS S3 Storage (used by 1 example workflow)
# Required for: File Upload workflow
# AWS_ACCESS_KEY_ID=your-aws-access-key-id
# AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
# AWS_REGION=us-east-1
# AWS_S3_BUCKET=your-bucket-name

# MongoDB (supported but not used in current examples)
# MONGODB_URL=mongodb://localhost:27017/your-database
# MONGODB_DATABASE=your-database-name

# ============================================================================
# Test Configuration
# ============================================================================
TEST_TIMEOUT_SECONDS=300
TEST_MAX_RETRIES=3
TEST_POLLING_INTERVAL_MS=2000

# CI/CD Configuration
CI_RUN_INTEGRATION_TESTS=true
CI_SKIP_SLOW_TESTS=false
